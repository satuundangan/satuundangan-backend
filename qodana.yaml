version: "1.0"
linter: jetbrains/qodana-js:latest
profile:
  name: qodana.recommended
exclude:
  - name: All
    paths:
      - dist
      - node_modules
      - build
      - coverage
      - .nyc_output
      - uploads
      - .idea
      - .github
